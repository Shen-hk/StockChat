# 聊天欢迎区架构试点与交接看板

> 更新：2026-09-12 ｜ Owner：架构集成人 ｜ 状态：**Wave 1（Chat 高风险状态机）四项全部落地**，代码与单测完成，待 Android/H5 人工回归

> 本文件是**架构重构的活进度看板**：每完成一刀就更新「实时进度」表与「当前主线」。
> 方案与路线图见 `39-项目级目标架构与演进蓝图`；单刀规格见 `40-Wave1下一刀决策与QuoteIsland短规格`、`41-实体交互状态层抽离与验证手顺`。

## 本轮短规格

- 目标：将聊天欢迎区从 `ChatPage` 的页面状态中抽为 Data / State / Coordinator / Component 的纵向切片，不改变文案、路由、动画数值与手势行为。
- 范围：欢迎词轮播、光标、示例卡两帧入场与 600ms 安全兜底、示例卡使用记录、"看行情" 的滑块/震动/跳转时序。
- 边界：不改 Drawer、Island、Voice、实体拖拽、输入框焦点、流式贴底；不新增 Gradle 模块或全局 Store。
- 验收：页面只转发生命周期和平台 Effect；异步任务在离页/销毁后不再写 UI；R4/R5 入场注册顺序保持；Android/H5 功能等价。

## 实时进度

| 项目 | 状态 | 证据 |
|---|---|---|
| 欢迎区持久化从页面迁出 | 已完成 | `chat/welcome/data/WelcomeStarterStore.kt` 仅依赖 `KeyValueStorage` |
| UI 状态与纯测试状态端口 | 已完成 | `ChatWelcomeStatePort` + `PlainChatWelcomeState`，避免单测伪造 Kuikly reactive observer |
| 定时、版本号与 lifecycle 收口 | 已完成 | `ChatWelcomeCoordinator.kt` 唯一拥有轮播、入场、市场跳转任务；`pageDidDisappear/pageWillDestroy` 均取消并递增版本 |
| Page 平台适配 | 已完成 | `ChatPage` 只处理 haptic 与 `Routes.MARKET` Effect，并将 reactive 读取留在 `WelcomeSection` 的取值闭包 |
| 自动化回归 | 已完成 | `:shared:testDebugUnitTest --tests com.kuikly.stockchat.chat.welcome.ChatWelcomeCoordinatorTest`：5 tests、0 failures |
| Android/H5 手测 | 待完成 | 新会话、历史会话、从详情返回、减弱动态、连续点"看行情" |
| 聊天流式贴底协调器 | 已完成 | `chat/scroll/state/ChatScrollCoordinator.kt`；4 项 fake-scheduler 测试通过 |
| CardSheet 状态机 | 已完成 | `chat/sheet/state/CardSheetCoordinator.kt`；2 项挂载/层级/卸载测试通过 |
| 组件物理归档 | 待完成 | 现有 `WelcomeSection` 仍在 `page/components/ChatScaffolding.kt`；需无行为迁到 `chat/welcome/component/ChatWelcomeSection.kt` |
| 附件与媒体面板状态机 | 已完成 | `chat/composer/state/ComposerAttachmentState.kt` 与 `MediaSheetCoordinator.kt`；3 项 fake-scheduler/附件单测通过，JS 编译通过 |
| Drawer 抽屉状态机 | 已完成 | `chat/drawer/state/ChatDrawerCoordinator.kt`；3 项时序测试通过（程序化开合 / 短左划收起 / 左缘过半展开）；ChatPage 移除约 140 行抽屉逻辑与手势判定 |
| QuoteIsland 灵动岛状态机 | 已完成 | `chat/island/state/QuoteIslandCoordinator.kt`（StatePort + Scheduler + Coordinator）；`IslandGestureMotion/Phase`、`ISLAND_ANIMATION_*` 由 `page/components` 迁入；阈值与时长逐值保留；5 项时序测试；ChatPage 5,897 → 5,493 行 |
| 实体交互状态机 | 已完成 | `chat/entity/state/EntityInteractionCoordinator.kt`；静止长按预览 / 长按转拖拽 / 投放目标 / 二义实体一体迁移（共享同一状态机）；10 项时序测试（含「术语拖拽后可再次长按」回归）；ChatPage → **5,273 行** |
| 灵动岛对比态样式回归修复 | 已完成 | `isCompareLobbyVisible` / `isTermLobbyVisible` 改读 StatePort observable（原读协调器镜像字段 → DSL 在 attr 内调用时不注册依赖，功能正常但岛样式不切换）；新增守护用例并反向验证有效 |
| 跨层反向依赖拆除 | 已完成 | `SourceStampLine` 下沉为 `cards.components.CardSourceStampLine`（doc 39 §2 边界问题第 3 条）；`AiChatMessage`/`AiMediaPart` 抽到 `data.provider.AiMessage`（Provider→chat 反向依赖），`ChatContext` 以 typealias 兼容 |
| 整库 Android 单测 | 已完成 | `:shared:testDebugUnitTest --rerun-tasks`：**215 tests、0 failures**（含 Drawer 3 + Island 8 + 实体交互 10 的新增用例）；`compileKotlinJs` 干净；`git diff --check` 干净 |

## 已验证的时序不变量

1. 欢迎示例卡仍然是 mounted → 32ms → presented；组件持续在预状态注册 `easeOut`，不写 0 时长 reset。
2. `pageDidDisappear` 取消所有 welcome 任务；若在 32ms 窗口离页，返回后会重新触发第二帧，不会留下透明卡片。
3. "看行情" 保持 240ms 后先震动再路由、420ms 后复位；离页后旧回调被版本号拦截。
4. Coordinator 不读取消息 observable；它只使用 Page 在生命周期/会话动作中传入的普通快照，避免定时器在 reactive closure 外读状态。

## 可交给他人的低耦合重构任务

每项必须独立分支/commit，合并前运行 common 单测与 Android 编译；不要与 `ChatPage` 的其它状态机混改。

### M1：欢迎组件物理归档（低风险）

- Owner 文件：`page/components/ChatScaffolding.kt` 中 Welcome 类型与 `WelcomeSection`，迁到 `chat/welcome/component/ChatWelcomeSection.kt`。
- 允许：只移动 Welcome UI、starter model 与私有渲染函数；更新 import。
- 禁止：改变文案、padding、颜色、`animate()` 调用位置、延迟和回调签名。
- 验收：冷启动欢迎卡与新会话欢迎卡均完整显示；`ChatWelcomeCoordinatorTest` 通过。

### M2：CardSheetState（已由架构集成人完成）

- 输出：`chat/sheet/state/CardSheetCoordinator.kt`；Page 保留 Card 的平台/路由回调，`CardGalleryPage` 通过兼容类型别名继续复用层级定义。
- 禁止：不要同时改实体拖拽、Drawer 或 Island 的 z-index/手势；预状态不可注册零时长动画。
- 验收：打开、下滑、上拉、关闭、快速连续打开均正常；Android 手测 CardSheet 层级高于聊天内容、低于 Drawer。

### M3：附件与媒体面板（中风险）

- Owner 文件：`ChatPage.kt` 的 `composerAttachments`、`mediaSheetMounted/presented`、原生媒体回调与附件预览渲染。
- 输出：`chat/composer/state/ComposerAttachmentState.kt`、`MediaSheetCoordinator.kt`；媒体原生结果通过 Page Effect 进入 state。
- 禁止：不改 TextArea focus、VoiceRecorder、发送协议；不要在 Timer/原生回调直接读 UI observable。
- 验收：图片/文档添加、删除、上限、面板开合、发送后清空；Android/H5 各走一次。

#### M3 本轮短规格（已完成，待 Android/H5 人工回归）

- 目标：迁出附件 ID 分配、添加/删除/清空、仅附件发送提示语，以及媒体面板的 `mounted → 16ms → presented`、`presented=false → 260ms → unmounted` 时序。
- 边界：`ChatPage` 继续解析原生 JSON、判断页面销毁、执行 toast/haptic/bridge/埋点，继续负责收键盘和发送协议；不触及 TextArea focus、VoiceRecorder、`ComposerMediaAction` 的 UI 文案或 `MediaActionSheetHost` 动效声明。
- R4/R5：弹层宿主的 `attr`/`animate` 原样保留；Coordinator 仅写 state，不从延迟回调读取 Kuikly observable。版本号会拦截快速开合与离页后的陈旧 timer。
- 自动化验收：fake scheduler 覆盖两帧打开、关闭等待、快速重开、附件上限和仅附件提示语；再运行 common unit tests 与 Kotlin/JS 编译。

## 当前主线（架构集成人）

1. **Wave 1 已收尾**：Drawer → QuoteIsland → EntityInteraction 三刀全部落地并通过全量单测（215 例、0 失败）。待办是把三刀的 Android/H5 手测补完并回填结果——实体交互与岛的清单见 `41-实体交互状态层抽离与验证手顺` §4 与 `40-Wave1下一刀决策与QuoteIsland短规格`。
2. M1 仅做欢迎 UI 的无行为物理归档；每次只合一个纵向切片。
3. **下一波（Wave 2）转场 `StockDetailPage`**（约 4,209 行）：先产出状态字段地图，再按 `39-项目级目标架构与演进蓝图` §9 Wave 2 的 5 个 Coordinator 顺序推进，不要按视觉区块搬文件。

## 下一纵切：ChatScrollState（已完成，待人工回归）

- 目标：把“发送后贴底、流式跟随、用户上滑接管、流结束宽限”的普通状态、版本号和轮询 Timer 从 `ChatPage` 收口到 Coordinator；Page 仅执行 `setContentOffset` 与既有 follow-up UI 回调。
- 不变量：首次发送仍是 animated 滚到底；流式/内容长高一律非动画；用户拖离底部后绝不抢回；流结束的 1.5s 宽限和流前 19s watchdog 保持原值。
- R1 方案：`streamState` 仅在 `vbind`/event 内读取并同步到 Coordinator 的普通镜像字段；Timer 不读取 `viewModel.streamState` 或任何 UI observable。
- 边界：不搬 Scroller ref、内容高度计算、回顶按钮、键盘事件、follow-up 的两帧 UI；不改变动画和 native scroll 参数。
- 验收：fake scheduler 已覆盖首次动画贴底、用户上滑接管、流结束宽限、离页取消；Android/H5 构建通过，待手测发送→流式→上滑→再次发送。
- 修正：用户主动上滑现在会取消尚未到期的发送贴底窗口，避免后台定时器把用户从历史消息强行拉回底部。

---

## Wave 1 后 ChatPage 剩余状态域（下一批候选）

ChatPage 现 **5,273 行**。已迁出：Welcome、ChatScroll、CardSheet、Composer/Media、Drawer、QuoteIsland、EntityInteraction。
仍未迁出（按收益排序，供后续切片选型）：

| 域 | 主要字段 / 现象 | 备注 |
|---|---|---|
| CompareInsight（AI 对比解读） | `compareCard`、`compareInsightState/Text/Error/PairKey/Version`、`compareCandidateKey/Symbol` | 含流式与取消，属"请求 + 流"型状态机 |
| 消息长按操作菜单 + 文本选择 | `messageActionMounted/Presented/X/Y/FollowUp`、`selectionMessageId`、`messageSelectionRefs`、`messageActionVersion` | 双态机（R4/R5）+ 原生选择手柄；与实体长按**不同域**，故未并入上一刀 |
| ComposerFocus + Voice | `composerFocusRecoveryPending`、录音/转写与振幅 | 与键盘、原生 bridge 耦合，风险独立 |
| 会话 UI 轻状态 | `expandedCardKey`、`focusedCardKey`、`repairingCardKey`、`drilledKeys`、`subThreads` | 多为纯 UI 记录，收益中等 |
| 其他零散 | `imagePreviewPath`、`glassMode*`、`historySearchQuery`、`chatBackToTop*` | 单字段级，可随邻近切片顺带收口 |

> 迁移注意：每刀开工前先 grep 该字段的**全部**引用，包括其他 coordinator 的 Effect 分支。
> EntityInteraction 一刀即因 Island 遗留的 `IslandEffect.ClearStockDrag/ClearTermDrag` 仍在写拖拽字段，
> 直到编译期才暴露 10 个错误。
